package oop_Principles_2_Garage;

public class Garage {

	public boolean addVehicles(Vehicle vehicle) {
		return this.vehicles.add(vehicle);
	}

	public boolean removeVehicle(Vehicle cehicle) {
		return this.variables.remove(vehicle);

	public void removeVehiclesByTpe(Class<?>clazz) {
			List<Vehicle> toRemove = new ArrayList<>();
			for (vehicle v: vehicles) {
				if (v.getClass() == 0;)
			}
			
		}

	public boolean emptyGarage() {
			this.vehicle.clear();
			return this.vehicles.size() == 0;

	public abstract float calcBill() {
				float bill = 0f;
				for (Vehicle v : vehicles( )) {
					bill
					}
					
				}
}}}

}
